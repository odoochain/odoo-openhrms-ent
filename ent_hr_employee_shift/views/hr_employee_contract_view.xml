<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="hr_contract_view_form" model="ir.ui.view">
        <field name="name">hr.contract.view.form.inherit.ent.hr.employee.shift
        </field>
        <field name="model">hr.contract</field>
        <field name="inherit_id" ref="hr_contract.hr_contract_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='resource_calendar_id']"
                   position="replace">
                <field name="resource_calendar_id" invisible="1"/>
            </xpath>
            <page name="information" position="after">
                <page name="sift_schedule" string="Shift Schedule">
                    <field name="shift_schedule_ids" nolabel="1">
                        <tree editable="1">
                            <field name="start_date"/>
                            <field name="end_date"/>
                            <field name="hr_shift_id"/>
                        </tree>
                    </field>
                </page>
            </page>
        </field>
    </record>
</odoo>
