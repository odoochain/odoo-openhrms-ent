<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Explicit wizard view definition -->
    <record id="ent_hr_employee_shift.generate_schedule_form"
            model="ir.ui.view">
        <field name="name">hr.shift.generate.view.form.ent.hr.employee.shift
        </field>
        <field name="model">hr.shift.generate</field>
        <field name="arch" type="xml">
            <form create="false" edit="false">
                <sheet>
                    <group>
                        <group>
                            <field name="hr_department_id"/>
                        </group>
                    </group>
                    <group>
                        <group>
                            <field name="start_date"/>
                        </group>
                        <group>
                            <field name="end_date"/>
                        </group>
                    </group>
                    <footer>
                        <button string="Generate" name="action_schedule_shift"
                                type="object"
                                class="oe_highlight"/>
                        <button string="Cancel" special="cancel"
                                class="oe_link"/>
                    </footer>
                </sheet>

            </form>
        </field>
    </record>

    <!-- Actions opening views on models -->
    <record model="ir.actions.act_window"
            id="ent_hr_employee_shift.generate_schedule_action_window">
        <field name="name">Employee Shift</field>
        <field name="res_model">hr.shift.generate</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

    <!-- Menu item -->
    <menuitem name="Shift" id="menu_shift_schedule_generate_id_menu"
              parent="menu_shift" groups="hr.group_hr_manager"/>
    <menuitem name="Generate Schedule"
              id="ent_hr_employee_shift.menu_shift_schedule_generate_id"
              parent="menu_shift_schedule_generate_id_menu"
              action="ent_hr_employee_shift.generate_schedule_action_window"
              groups="hr.group_hr_manager"/>

</odoo>